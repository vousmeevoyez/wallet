"""
    Test Scheduler Services
"""
from datetime import datetime, timedelta

from app.api.schedulers.modules.scheduler_services import SchedulerServices


"""
def test_add_schedule():
    executed_at = datetime.now() + timedelta(seconds=10)
    result = SchedulerServices().add("greet_user", "Hi Jessica Jung", executed_at)
    print(result)

def test_show_all_schedule():
    executed_at = datetime.now() + timedelta(seconds=10)
    result = SchedulerServices.show_all()
    print(result)
"""
